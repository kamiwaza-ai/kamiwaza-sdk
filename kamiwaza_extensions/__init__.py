"""Kamiwaza extension developer tools."""

# This is the kz-ext CLI version, intentionally separate from kamiwaza-sdk.
# The SDK is versioned as kamiwaza-sdk (e.g. 0.11.0); the extensions CLI
# starts at 0.1.0 and follows its own release cadence.
__version__ = "0.1.0"
