@AGENTS.md

---

**Do not add content to this file.** `CLAUDE.md` is intentionally nothing but the `@AGENTS.md` import, so there is a single source of truth and zero drift between agent docs. Put shared guidance in `AGENTS.md`. For Claude-only guidance, see "Agent-specific instructions" in `AGENTS.md` — use `.claude/rules/`, never this file.
